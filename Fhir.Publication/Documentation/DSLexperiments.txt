select main.html >> template template.html >> stash $a
select documentation1.html >> template xxx template.html >> attatch $a
select documentation2.html >> template template.html >> attatch $a
select different.html >> template template.html >> save .html

select [$a]*.* >> concatenate >> save .html

select [output]*.bla >> etc.

select *.blie

---

filter documentation.html >> template template.html >> save .html

filter documentation1.html >> template template.html >> save .html
filter documentation2.html >> template template.html >> save .html
filter structure.*.html -output >> template template.html >> save .html
addindex




filter [output]*.html >> attach [A]$-search.xml "Search" >> attach [A]$-binding.xml "Binding" 
		>> resourcepagemerger >> save $.html

(in resourcepage merger Processor) 

Process(Document input, Stage output)
{
	var searchDoc = input.Attachment("Search");
	var bindingDoc = input.Attachment("Binding");
	
	var merged = // magic;
	var summary = // more magic;
	
	output.Add(merged);
}



(in include Processor)

Process(Stage input, Stage output)
{
	var 
}

---------------------------------

---
select * >> process >> process2 >> store $A
select *.html -recursive -output >> template $A.template.html >> save .html

